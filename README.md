# Beginning_JavaScript-HTML
One HTML file which have only the main a structure of code in that language. By another way, in a JavaScript i wrote code about primite typs, arrays and objects in JavaScript.  
